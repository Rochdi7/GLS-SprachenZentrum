<div class="text-center text-muted py-5">
    <div class="mb-3">
        <i class="ph-duotone {{ $icon ?? 'ph-folder-open' }}" style="font-size: 2.25rem;"></i>
    </div>
    <h6 class="mb-2">{{ $title ?? 'Aucune donnee' }}</h6>
    <p class="mb-0">{{ $message ?? 'Aucun element a afficher pour le moment.' }}</p>
</div>
