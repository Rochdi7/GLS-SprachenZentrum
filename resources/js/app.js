// Vite entry file. The backoffice template loads its scripts directly from
// public/build/js/* (copied via vite-plugin-static-copy), so this entry is
// intentionally empty — it just satisfies Vite's input requirement.
